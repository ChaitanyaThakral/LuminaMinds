[pytest]
testpaths = tests
addopts = --cov=notebooks --cov=analytics --cov=dashboard --cov=experiments --cov-report=term-missing --cov-report=html:tests/coverage_html -v
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
